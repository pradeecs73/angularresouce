(function() {
    'use strict';
    angular.module('mean.assets').factory('Assets', Assets);
    Assets.$inject = ['$http', '$q'];

    function Assets($http, $q) {
        return {
            
        };
    }


})();
